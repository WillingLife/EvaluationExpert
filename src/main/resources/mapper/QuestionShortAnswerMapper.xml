<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smartcourse.mapper.QuestionShortAnswerMapper">
    <insert id="insert">
        insert into question_short_answer (question_id, answer, criteria)
        values (#{questionId}, #{answer}, #{criteria})
    </insert>
</mapper>